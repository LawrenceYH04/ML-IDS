# Datasets — not distributed via git (see DATA.md). CSE-CIC-IDS2018 raw CSVs
# plus all processed/feature arrays, ~11 GB total.
data/

# Large trained models — regenerate via notebooks (see DATA.md).
# random_forest.pkl ~2 GB, xgboost.pkl ~117 MB, feat_sel_X_sample.npy ~123 MB.
# Small scalers, imputers, autoencoders and JSON configs are kept.
models/random_forest.pkl
models/xgboost.pkl
models/feat_sel_X_sample.npy

# Derived prediction / probability arrays — regenerate via evaluation notebooks.
results/*.npy

# Report / write-up artefacts — submitted separately, not part of the code deliverable.
*.docx
report_methodology_results_UPDATED*.md
SESSION_FINDINGS.md
report_build/
build/make_docx.py
build/make_dashboard_docx.py
build/make_highlighted_report.py

# Pre-restyle backups of the figures in results/ (superseded).
results/legacy_style/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Backups / editor / OS
*.bak
*.bak-*
.DS_Store
.vscode/
.idea/
